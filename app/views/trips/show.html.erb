<%= @trip.name %>
<%= @trip.starting_date %>
<%= @trip.end_date %>
<% @trip.steps.each do |step| %>
    <%= step.name %>
<% end %>

<%= link_to "Delete",
    trip_path(@trip),
    method: :delete,
    data: { confirm: 'Are you sure?' } %>
